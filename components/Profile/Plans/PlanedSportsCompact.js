

const PlannedSportsCompact = (props) =>{

   const groupedEntries = props.groupedEntries


   console.log(groupedEntries)
    return(

        <div>

            compact
        </div>
    )
}

export default PlannedSportsCompact





/ACTUAL!!!
/*

0. zu beginn werden alle tagebucheinträge (filteredEntries) des aktuellen Jahres geladen. 
1. Wenn der User über den select ein anderes Jahr auswählt, dann alle filteredEntries aus eben diesem Jahr.
2. Wenn der User einen bestimmten Monat anklickt, werden alle filteredEntries aus dem ausgewählten Jahr 
   (per default das aktuelle) in eben diesem Monat angezeigt. 
3. Wenn der User den Monat ein 2. Mal anklickt, werden einfach wieder alle aus dem ausgewählten jahr angezeigt



0. At the beginning, all diary entries (filteredEntries) of the current year are loaded. 
1. If the user chooses a different year via select, then all filteredEntries from that year are loaded. 
2. If the user clicks on a specific month, all filteredEntries from the selected year 
   (by default the current one) are displayed in that month. 
3. If the user clicks on the month a second time, all filteredEntries from the selected year are displayed again

Translated with DeepL.com (free version)



*/


/*

declare fetchSportsData function @ Navigation.js && AddEntryForm.js in a seperate file!
*/


/*

@Calendar.js => update the redux state in calendarReducer.js, with the date object clicked by the user
you may need redux thunk???


*/

// BOARDS.JS -> ADDENTRYFORM
//revise the AddEntryForm component to make the slide effect work in transition groups
// form should work :)


// fetching supabase table data at Navigation.js -> better at index.js?+


// in DetailsPage! the path can be duplicated if the user assigns the same title more than once. Solve this problem 
//(possibly by using the id next the path? uuidv()??)


/*

fix the navigation and its form. 
User should only be able to add sports via the form in Navigation.js 
that are not yet available in Navigation.js. 
Avoid potential duplicates in the navigation bar! 
If the user is about to create a duplicate, give him the information
that he should go to the sport via the navigation bar and then
click on “add entry”.

*/